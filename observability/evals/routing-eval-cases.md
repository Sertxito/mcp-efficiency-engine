Casos base:

- bug login -> CodeGraph + Caveman (full)
- SLA -> Azure RAG Builder + fuentes
- arquitectura -> Graphify
- legacy -> GitNexus

Suite ejecutable JSON-first:

- `observability/evals/routing-eval-cases.json`

Runner:

- `python .\scripts\run-routing-evals.py`
